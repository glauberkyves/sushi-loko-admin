<form method="post" id="form-franquia" class="cmxform form-horizontal tasi-form" novalidate="novalidate">
    <div class="form-group">
        <label class="col-sm-2 control-label col-lg-2" for="noFranquia">Tipo Template</label>
        <div class="col-lg-4">
            {{ formText('idTipoTemplate', entity.getIdTipoTemplate().getIdTipoTemplate(), {
                class: 'form-control required'
            }) }}
        </div>
    </div>
    <div class="form-group">
        <br>
        <button type="submit" class="btn btn-primary">
            <i class="fa fa-plus"></i>
            Salvar
        </button>
        <a type="button" href = "{{ path('super_franqueador_template') }}" class="btn btn-danger">
            Cancelar
        </a>
    </div>
</form>